import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.ra1g.radiorpi',
  appName: 'radiorpi',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
